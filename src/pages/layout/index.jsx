import React from 'react';
import { RouterView } from '../../utils/router';

function Layout() {
  return (
    <div>
      <span>Layout</span>
      <div>
        <RouterView />
      </div>
    </div>
  );
}

export default Layout;
